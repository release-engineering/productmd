server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;

    # Enable directory listing for compose tree browsing
    autoindex on;

    location / {
        try_files $uri $uri/ =404;
    }
}
